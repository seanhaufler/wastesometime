var Timer = Backbone.View.extend({

  /**
   * @param {{
   *   type: (string),
   *   title: (string),
   *   wordCount: (number|undefined),
   *   url: (string)
   * }} options
   */
  initialize: function(options) {

  },

  render: function() {

  }
}
